-- CREATE TABLE "main"."SvtTestSetup" (
-- "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
-- "name" varchar(100),
-- "defaultConfigId" integer DEFAULT null,
-- "generalLocation" main."wpGeneralLocation"
-- );
--
-- CREATE TABLE "main"."SvtTestSetupEquipList" ("setup_id" integer, "equip_id" integer);
--
-- CREATE TABLE "main"."SvtTestSetupConfig" (
-- "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
-- "baseId" integer NOT NULL,
-- "setupId" integer NOT NULL,
-- "body" JSON NOT NULL,
-- "date" date DEFAULT (CURRENT_DATE),
-- "Note" text
-- );
--
-- ALTER TABLE "main"."SvtTestSetup" ADD FOREIGN KEY ("defaultConfigId") REFERENCES
-- "main"."SvtTestSetupConfig" ("id");
--
-- ALTER TABLE "main"."SvtTestSetupEquipList" ADD FOREIGN KEY ("setup_id") REFERENCES
-- "main"."SvtTestSetup" ("id");
--
-- ALTER TABLE "main"."SvtTestSetupEquipList" ADD FOREIGN KEY ("equip_id") REFERENCES
-- "main"."Equipment" ("id");
--
-- ALTER TABLE "main"."SvtTestSetupConfig" ADD FOREIGN KEY ("baseId") REFERENCES
-- "main"."SvtTestSetupConfig" ("id");
--
-- ALTER TABLE "main"."SvtTestSetupConfig" ADD FOREIGN KEY ("setupId") REFERENCES
-- "main"."SvtTestSetup" ("id");
-- INSERT INTO
-- main."SvtTestSetup" (name, "generalLocation")
-- VALUES
-- ('ts_mit', 'CERN_186_R_E10'),
-- ('analog_monitor_prober_mit', 'CERN_186_R_E10');

